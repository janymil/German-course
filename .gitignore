# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables — never commit
.env
.env.local

# Pre-generated audio (large binaries — regenerate with scripts/generate-story-audio.js)
public/audio/stories/*.mp3

# Personal progress — never commit, lives on your machine
progress.json
progress.backup.json

# Large ANKI binary files — too big for GitHub
ANKI/*.apkg
ANKI/unzipped/
ANKI/collection.anki2

# Ebook source folder — large mp3/media files, tracked via public/ only
ebook/

# Backup files
*.bak
