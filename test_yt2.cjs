const https = require('https'); https.get('https://www.youtube.com/watch?v=4-eDoThe6qo', (res) => { let d = ''; res.on('data', c => d+=c); res.on('end', () => { let match = d.match(/"captionTracks":\s*(\[.*?\])/); if (match) { let url = JSON.parse(match[1])[0].baseUrl; https.get(url, (r) => { let x = ''; r.on('data', c => x+=c); r.on('end', () => console.log(x.substring(0, 500))); }); } }); });
